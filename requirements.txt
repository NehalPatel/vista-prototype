# Web app
flask>=2.0

# Video / pipeline (YOLO, download, render)
ultralytics>=8.0
opencv-python>=4.5
pytube
yt-dlp

# Face pipeline (instruction_face.txt) – for face detection in videos, uncomment insightface
# insightface  # optional: pip install insightface (may need onnxruntime-gpu for GPU)
onnxruntime
opencv-python>=4.5
numpy
scikit-image
tqdm
scipy
