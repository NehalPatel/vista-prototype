# Web app
flask>=2.0

# Video / pipeline (YOLO, download, render) – YOLO uses PyTorch for inference
torch
torchvision
ultralytics>=8.0
opencv-python>=4.5
pytube
yt-dlp

# Face pipeline – required for face detection in the web UI
insightface
onnxruntime
opencv-python>=4.5
numpy
scikit-image
tqdm
scipy
scikit-learn

# GPU: Default pip installs are often CPU-only. For CUDA GPU:
#   PyTorch (YOLO): https://pytorch.org – e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   ONNX (InsightFace): pip install onnxruntime-gpu  (replace onnxruntime)
