# MongoDB connection (set to enable saving detection results for search engine)
# Example: mongodb://localhost:27017 or your Atlas SRV URL
MONGODB_URI=mongodb://localhost:27017

# Optional: custom DB name (defaults to vista_search if not set)
VISTA_DB_NAME=vista_search

# Flask debug (optional)
FLASK_ENV=development
FLASK_DEBUG=1
